---
language: python
python: "2.7"

env:
  - ANSIBLE_VERSION=1.5
  - ANSIBLE_VERSION=1.6
  - ANSIBLE_VERSION=1.7
  - ANSIBLE_VERSION=1.8

before_install:
  - sudo apt-get update -qq

  # Remove bash
  - sudo apt-get remove --purge bash || true

install:
  # Install Ansible.
  - pip install ansible==$ANSIBLE_VERSION

  # Add ansible.cfg to pick up roles path.
  - printf "[defaults]\nroles_path = ../" > ansible.cfg

script:
  # Check the role/playbook's syntax.
  - ansible-playbook -i tests/inventory tests/test.yml --syntax-check

  # Run the role/playbook with ansible-playbook.
  - ansible-playbook -i tests/inventory tests/test.yml --connection=local --sudo -vvvv

  # Run the role/playbook again, checking to make sure it's idempotent.
  - >
    ansible-playbook -i tests/inventory tests/test.yml --connection=local --sudo
    | grep -q 'changed=0.*failed=0'
    && (echo 'Idempotence test: pass' && exit 0)
    || (echo 'Idempotence test: fail' && exit 1)

notifications:
  email: false
  hipchat:
    rooms:
      secure: GXrFq8NzITvzWqku5ospnE/ksqgnJM47o3/MxsKvS7dWzrTVFQrUNEVBcgK8bS51F6lJKmcK9YAif4qQR8AM4UPNRrPThR6NwEMwZMhXFqAwgf2RKzOfH1mrJOX26rm48Fm9jD1Iu7Pe8uTGLm52Ak0qZAjcx736tcUE7Oq8ujk=
